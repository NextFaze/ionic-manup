import { expect } from 'chai';

describe('Manup Spec', () => {

    it('The world is sane', () => {
        expect(2+2).to.equal(4);
    })

    describe('Evaluate', () => {

    });

    describe ('getMetadata', () => {

        before('construct the service', () => {

        })

        it ('should GET the data from the remote url')
        it ('should return an observable')
    })

    describe('getPlatformData', () => {
        it('should return the IOS branch if the plaform is IOS');
    })

    describe('evaluate', () => {
        it('should return an alert type');
    })
})
